meta {
  name: tables
  type: http
  seq: 2
}

post {
  url: {{host}}/api/v1/connections/{{connectionName}}/tables
  body: json
  auth: inherit
}

body:json {
  ["spotify"]
}

vars:pre-request {
  connectionName: default
}

settings {
  encodeUrl: true
  timeout: 0
}
