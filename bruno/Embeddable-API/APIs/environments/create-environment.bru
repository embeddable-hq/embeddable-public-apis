meta {
  name: Create environment
  type: http
  seq: 4
}

post {
  url: {{host}}/api/v1/environments
  body: json
  auth: inherit
}

body:json {
  {
      /* 
       * learn more [here](https://docs.embeddable.com/data/environments) 
       */
      "name": "my-environment",
      "datasources": [
        { "data_source": "primary_db", "connection": "my-postgres" },
        { "data_source": "warehouse", "connection": "my-bigquery" }
      ]
    }
}

settings {
  encodeUrl: true
}
